/**
 * Application entry component for the investigation game.
 */

import { GameDashboard } from '@/components/GameDashboard';

export default function App() {
  return <GameDashboard levelNumber={1} />;
}
